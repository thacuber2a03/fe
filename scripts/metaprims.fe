(do
  (let add (fn (a) (fn (b) (eval (+ a b)))))

  (let increment (add 1))
  (print (increment 2))
  (print (increment 5))

  (print (type "string"))
  (print (type '(1 2 3 4)))
  (print (type (fn (x) (* x x)))))
